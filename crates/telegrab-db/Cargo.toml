[package]
name = "telegrab-db"
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }
description = "Database access layer for telegrab"

[lib]
path = "src/lib.rs"

[dependencies]
telegrab-model = { path = "../telegrab-model" }
sqlx = { workspace = true, features = ["postgres", "runtime-tokio", "macros"] }
sqlx-postgres = { workspace = true, features = ["uuid", "time"] }
time = { workspace = true }
